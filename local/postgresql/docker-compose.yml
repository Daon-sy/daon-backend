version: '3.7'
services:
   db:
      container_name: postgresql
      image: postgres
      ports:
         - "5432:5432"
      volumes:
         - ./db/data:/var/lib/postgresql/data
      env_file: .env
      restart: always

networks:
   local-net:
      external: true
